# Lane detection modules package
# Each module contains one specific algorithm
# Import order: color_filter -> preprocessor -> roi_calibrator
#               -> histogram -> sliding_window -> lane_fitter -> ldw
# Main coordinator: lane_detector.py
